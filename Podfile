# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CovidContactTracking' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CovidContactTracking

  pod 'SwiftEx/Int', :git => 'https://gitlab.com/seriyvolk83/SwiftEx.git', :branch => 'covid19'
  pod 'AppCenter'
#  pod 'AzureNotificationHubs-iOS'
end
